# Bugs — Resolved Issues


| #   | Bug                                                                             | Root Cause                                                                                                               | Fix                                                                                 | Commit               |
| --- | ------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------- | -------------------- |
| 001 | Agent re-fetches branch/job every turn                                          | Agentic loop rounds not saved to chat history — next user message only saw first response                                | Save feedback + response to chat history after each agentic loop round              | `e76ec90`            |
| 002 | Session Memory never reached LLM                                                | `build_prompt()` strips system messages — `[Session Memory]` block was discarded                                         | Extract memory block from system message, append to `agent.system_prompt`           | `cc99409`            |
| 003 | Agentic loop didn't capture `[REMEMBER:]` tags                                  | Tags emitted in round 1 never captured, rounds 2-4 used stale system_context                                             | Capture tags + rebuild context each round in `chat_repl.py`                         | `dd91f40`            |
| 004 | Scratchpad missing in skill/Q&A/delegation paths                                | Only main message path had scratchpad injection                                                                          | Inject fresh scratchpad in all `_stream_with_spinner` call sites                    | `718b91e`            |
| 005 | Agent re-triggers build during deploy                                           | System prompt said "ALWAYS re-build when user asks" — interpreted "Build and deploy" as two builds                       | Clarify re-build only on explicit "build again"; `DEPLOY ≠ BUILD` rule              | `6896a4c`, `5231671` |
| 006 | Deploy missing `env_name` parameter                                             | Deploy skill didn't enforce all params required                                                                          | Added "ALL parameters REQUIRED, missing env_name will fail"                         | `75e85f3`            |
| 007 | Deploy job 404 — wrong path guessed                                             | Deploy folder is subfolder inside build jobs, not separate folder                                                        | Learn folder tree via `[REMEMBER:jenkins_folders]`, check subfolder first           | `890d3fe`, `69029b7` |
| 008 | Agent fetched build job params instead of deploy job params                     | Deploy skill said "DEPLOY_JOB_PATH" but agent picked build_job from memory                                               | Explicit "NOT the build job" at every deploy step                                   | `5231671`            |
| 009 | curl JSON not quoted — "Port number" error                                      | Agent dropped single quotes around `-d` JSON payload                                                                     | Added CORRECT/WRONG examples in system prompt                                       | `dfe54d8`            |
| 010 | No build progress in REPL during polling                                        | Blocking endpoint + `subprocess.run` buffers all output                                                                  | Streaming NDJSON endpoint + `Popen` + `readline()` + `curl -N`                      | `da5a735`, `5872a77` |
| 011 | Ctrl+C exit threading exception                                                 | Python 3.14 thread pool join interrupted by second Ctrl+C                                                                | `os._exit(0)` after cleanup                                                         | `8426cef`            |
| 012 | `job_path` vs `job_name` mismatch                                               | LLM hallucinated wrong parameter name                                                                                    | Added `job_path` as alias in Pydantic model                                         | `da5a735`            |
| 013 | LLM hallucinated entire multi-round conversation (10K chars, fake H: responses) | `build_prompt()` formats as `Human:/Assistant:` text — LLM continued generating both sides                               | Hard stop: "NEVER generate Human: lines, ONE bash block then STOP IMMEDIATELY"      | `773b7cc`            |
| 014 | Agent said "no rules file" despite rules existing                               | Rules file has "Saved Commands" not `JENKINS_BUILD_JOB` — agent looking for job config, not saved commands               | Add `JENKINS_BUILD_JOB=...` to rules file                                           | —                    |
| 015 | "command already in rules" but still asking permission                          | `autorun.yaml` never loaded by Python code — "mutation detected" comes from Claude CLI's own permission system, not ours | Claude CLI backend has its own approval layer that ignores our autorun.yaml         | —                    |
| 016 | Edit option executes user text as shell command                                 | User typed feedback in Edit prompt, got executed as bash                                                                 | Edit should feed text back to agent as context, not run as command                  | —                    |
| 017 | ANSI escape codes rendering as `?[33m` in chat output                           | `patch_stdout` was using `raw=True` which bypasses ANSI processing                                                       | Set `raw=False` in `patch_stdout` call to enable proper escape sequence rendering   | —                    |
| 018 | `green` UnboundLocalError in `chat_commands.py`                                 | Local variable `green` shadowed the imported `green` function from Rich                                                  | Renamed local variable to avoid shadowing the import                                | —                    |
| 019 | Questionnaire options rendering horizontally instead of vertically              | `\r\n` line endings in raw mode collapsed options onto one line                                                          | Use platform-appropriate line endings and flush output buffer                       | —                    |
| 020 | Arrow key echo `^[₹^[` in command panel                                         | `os.read()` was used for input instead of `sys.stdin` which handles terminal escape sequences                            | Switch to `sys.stdin` reading with proper terminal mode handling                    | —                    |
| 021 | `[SKILL:]` tags showing in agent response output                                | Skill loading tags not stripped from final rendered response                                                             | Added post-processing step to strip `[SKILL:...]` tags before display               | —                    |
| 022 | Response text not indented in agent box                                         | Agent response box used flat text without left margin                                                                    | Added consistent indentation to all lines within the agent response panel           | —                    |
| 023 | Spinner hiding input prompt during thinking                                     | Spinner animation overwrote the prompt line when agent was processing                                                    | Move spinner to a dedicated line above the input prompt                             | —                    |
| 024 | SQL injection in LIMIT clause (`db_client.py`)                                  | User-controlled value interpolated directly into SQL LIMIT clause                                                        | Use parameterized query for LIMIT value instead of string interpolation             | —                    |
| 025 | Path traversal via `X-Repo-Path` header (`app.py`)                              | No validation on repo path header — attacker could access files outside project                                          | Added `Path.resolve()` + bounds check to ensure path stays within allowed directory | —                    |
| 026 | SSRF via Slack `response_url` (`slack_bot.py`)                                  | Slack response URL not validated — attacker could redirect to internal services                                          | Added URL validation to ensure `response_url` points to `hooks.slack.com` only      | —                    |
| 027 | Path traversal in `watch_mode` and terraform `var_file`                         | File paths from user input not sanitized before file operations                                                          | Added path normalization and bounds checking for all user-supplied file paths       | —                    |
| 028 | Connection state corruption in `db_client` `table_indexes`                      | Shared connection object modified concurrently during index listing                                                      | Use connection-per-request pattern to avoid shared state corruption                 | —                    |
| 029 | Missing `commit_id` validation in `pr_review_client`                            | Commit ID accepted without format validation — could inject arbitrary git refs                                           | Added regex validation for commit ID format (40-char hex SHA)                       | —                    |


