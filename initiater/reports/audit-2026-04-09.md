Auditing 4 dimension(s): documentation, security, testing, workflow
Scanning project state...
Calling LLM (local)...
Analyzing project quality against 4 audit dimensions -- reading key files for evidence-based evaluation.Now I have comprehensive evidence. Let me compile the full audit report.

---

## Project Quality Audit Report

**Auditing code-agents project against 4 dimensions (documentation, security, testing, workflow) -- 35 rules total.**

---

### Documentation [CRITICAL]

| Status | Rule | Details |
|--------|------|---------|
| **PASS** | README.md "Included Agents" table lists all active agents | README lists all 13 agents matching the 13 active YAMLs in `agents/` |
| **PASS** | Agents.md has a section for every active agent | All 13 agents have dedicated `## Section` headings in Agents.md |
| **WARN** | agent_router.yaml system prompt lists all specialist agents | **No `agent_router.yaml` exists.** Routing lives in `auto_pilot.yaml` system prompt, which lists 12 specialists + "agent-router" (a skill, not an agent). Documentation references a file that doesn't exist. |
| **WARN** | API endpoint documentation matches actual routes in code | README lists REST APIs (`/pipeline/*`, `/git/*`, etc.) broadly. 17 routers exist in `code_agents/routers/` (including `atlassian_oauth_web`, `elasticsearch`, `slack_bot`) but README only documents ~10 route groups. Elasticsearch and Slack Bot routes undocumented in README. |
| **WARN** | Environment variable table in README covers all vars used in code | README env table has ~14 vars. `.env.example` has ~30+ vars (e.g., `ELASTICSEARCH_URL`, `CODE_AGENTS_SANDBOX`, `CODE_AGENTS_HTTP_ONLY`, `CURSOR_API_URL`, `ATLASSIAN_OAUTH_*` all missing from README). README says "Full list: `.env.example`" which is a reasonable redirect but the table itself is incomplete. |
| **WARN** | YAML configuration reference documents all supported fields | Agents.md shows a YAML example with common fields but omits `stream_tool_activity`, `include_session`, `extra_args.mode` details, and env var expansion syntax for `backend`/`model`. |
| **PASS** | Troubleshooting section is current | No troubleshooting section exists, but `code-agents doctor` serves this role. No references to removed features found. |
| **PASS** | Quick Start instructions work on a fresh clone | Install steps (clone → install.sh → init → start → chat) are coherent and reference real commands. |

---

### Security [CRITICAL]

| Status | Rule | Details |
|--------|------|---------|
| **PASS** | No hardcoded API keys or secrets in source code | Thorough search found zero real secrets. Test files use synthetic keys (`sk-ant-123`, `sk-abcdefg...`). All production values via `os.getenv()`. |
| **PASS** | .gitignore excludes .env, credentials, and key files | `.gitignore` covers `.env`, `.env.*`, `.webui_secret_key`, `*.py[cod]`. Solid coverage. |
| **PASS** | API keys use environment variable injection (`${VAR}` syntax) | All 13 agent YAMLs use `${CURSOR_API_KEY}` syntax. `config.py` handles expansion. |
| **PASS** | permission_mode is set appropriately per agent | All 13 agents use `permission_mode: default` (read-only). None use `bypassPermissions`. |
| **WARN** | Input validation on chat completion requests | `models.py` uses Pydantic with `extra="ignore"` — lenient but functional. Content validator handles multimodal input. No strict schema enforcement for message format (role enum, content type). |
| **FAIL** | No path traversal via agent name or cwd parameter | **Agent name**: implicitly safe (whitelist lookup in `agent_loader`). **CWD**: `req.cwd` is passed **unsanitized** to `subprocess_exec(cwd=...)` in `backend.py:254,442`. No `Path.resolve()`, no `normpath()`, no allowlist check. An attacker could set `cwd` to any directory. |
| **FAIL** | CORS is configured (not wildcard in production) | `app.py:148` — `allow_origins=["*"]` with `allow_credentials=True`. This is the most dangerous CORS combination: allows any origin to make credentialed requests. |
| **WARN** | TLS verification is on by default | ArgoCD: `ARGOCD_VERIFY_SSL` defaults to `1` (good). Atlassian OAuth: `CODE_AGENTS_HTTPS_VERIFY=0` documented as escape hatch. Elasticsearch: `ELASTICSEARCH_VERIFY_SSL=1` default. Generally good, but opt-out exists. |
| **WARN** | Dependencies are pinned and free of known vulnerabilities | Poetry lockfile pins versions, but no `safety` / `pip-audit` scan is configured in CI. Cannot verify vulnerability status. |
| **WARN** | OAuth tokens are stored securely | Atlassian OAuth tokens stored in-memory during session. No evidence of plain-text logging, but no explicit audit of token handling in all code paths. |

---

### Testing [CRITICAL]

| Status | Rule | Details |
|--------|------|---------|
| **PASS** | Test files exist for core modules (config, backend, stream, routers) | `test_backend.py`, `test_stream.py`, `test_routers.py` (+ 3 more router test files), `test_config_drift.py`, `test_env_loader.py` all present. 163+ test files. |
| **PASS** | pytest is configured in pyproject.toml | `[tool.pytest.ini_options]` with `asyncio_mode = "strict"`. Coverage config present under `[tool.coverage.*]`. |
| **PASS** | Tests can run with `poetry run pytest` | Config exists; 3759 tests claimed in badges. Makefile has `test`, `test-v`, `test-cov` targets. |
| **PASS** | Agent YAML loading is tested | `test_skill_loader.py`, `test_rules_loader.py` exist. Config loading tested via multiple test files. |
| **PASS** | API endpoints have integration tests | `test_routers.py`, `test_routers_coverage.py`, `test_routers_extended.py`, `test_routers_small.py` — 4 router test files. |
| **PASS** | Streaming responses are tested | `test_chat_streaming.py` and `test_stream.py` both present. |
| **PASS** | Session management is tested | `test_session_scratchpad.py`, `test_chat_history.py`, `test_chat_history_extra.py`, `test_chat_state.py` present. |
| **FAIL** | CI pipeline runs tests on every PR | **No CI config found.** No `.github/workflows/`, no `bitbucket-pipelines.yml`, no `Jenkinsfile`, no `.gitlab-ci.yml`. Tests only run locally or via the jenkins-cicd agent (manual trigger). |
| **WARN** | Test coverage is measured and reported | Coverage config exists in `pyproject.toml` (`[tool.coverage.*]`). `make test-cov` target available. But no coverage threshold enforcement or reporting in CI (since there's no CI). |

---

### Workflow [CRITICAL]

| Status | Rule | Details |
|--------|------|---------|
| **WARN** | Every agent in agents/*.yaml is listed in agent_router.yaml | **`agent_router.yaml` does not exist.** Auto-pilot's system prompt lists 12 specialists + "agent-router" (a skill). Functional equivalent exists but file referenced in rules is missing. |
| **PASS** | Every agent is listed in README.md "Included Agents" table | All 13 agents present in the Agents table with roles and permissions. |
| **WARN** | Every agent is listed in README.md "Option B" connections table | README does not have an "Option B connections table." There's an "Option B: Claude API" section in Agents.md but it's about backend config, not a per-agent table. |
| **PASS** | Every agent is listed in README.md "Project Structure" tree | Project structure mentions `agents/<name>/` with "14 agent subfolders (13 agents + _shared)". |
| **PASS** | Every agent is documented in Agents.md with its own section | All 13 agents have `## Section` headings with endpoint, permission, description, skills, and requirements. |
| **WARN** | agent_router.yaml specialists list matches actual agent files | No `agent_router.yaml`. Auto-pilot lists specialists but includes "agent-router" which is a skill, not an agent — slight mismatch. |
| **PASS** | Agents.md "Maintenance" section is up to date | Maintenance checklist present with 7 steps. References correct files (`chat.py`, `cli.py`, `auto-pilot.yaml`). |
| **WARN** | New agent YAML follows the same field ordering | All 13 YAMLs use `permission_mode: default`, but README claims some agents use `acceptEdits` (code-writer, code-tester, test-coverage, qa-regression) while **actual YAMLs all show `default`**. Documentation and code are **out of sync** on permission modes. |

---

## Summary

| Metric | Count |
|--------|-------|
| **Total rules** | 35 |
| **PASS** | 20 |
| **FAIL** | 3 |
| **WARN** | 12 |

### Critical Failures

| Dimension | Rule | Impact | Recommended Fix |
|-----------|------|--------|-----------------|
| **Security** | Path traversal via `cwd` parameter | `req.cwd` passed unsanitized to `subprocess_exec()` — attacker can execute commands in any directory | Add `Path(cwd).resolve()` + allowlist check in `stream.py` before passing to `backend.py` |
| **Security** | CORS wildcard with credentials | `allow_origins=["*"]` + `allow_credentials=True` enables CSRF from any origin | Restrict to `localhost` + configurable origins via env var |
| **Testing** | No CI pipeline | 3759 tests exist but no automated gate on PRs — regressions can ship uncaught | Add `bitbucket-pipelines.yml` with `poetry run pytest` step |

### Top Warnings (High-Impact)

1. **Documentation ↔ Code drift on permission_mode**: README/Agents.md say code-writer, code-tester, test-coverage, qa-regression use `acceptEdits`, but all 13 YAMLs use `default`. Either docs or YAMLs need updating.
2. **Missing `agent_router.yaml`**: Multiple rule files reference this, but it doesn't exist. Routing lives in auto-pilot's system prompt + skills.
3. **Incomplete env var documentation**: README table covers ~14 of 30+ supported vars.
4. **No dependency vulnerability scanning**: No `pip-audit` or `safety` in any workflow.
